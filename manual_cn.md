# ReFocus 使用手册

## 快速开始

1. 在左侧 **提示词框** 中输入你想要生成的内容
2. 点击 **生成** 按钮
3. 等待结果出现在右侧画廊中

就这么简单。所有复杂的功能都是可选的，你只需要提示词就能开始。

---

## 控制图像生成

如果你希望对生成过程有更多控制，勾选 **输入图像**，下方会展开四个工具面板。

### 放大与变体（UOV）

上传一张图片后，你可以：

- **变体（Vary）**：基于原图生成相似但不完全一样的版本
  - 轻微变化 → 小幅度调整
  - 强烈变化 → 更大胆的改动
  - 自定义 → 手动控制变化强度（0-1，数值越高变化越大）

- **放大（Upscale）**：将图片放大，同时补充细节
  - 选择放大倍数（0.25x - 4x）
  - 启用“快速模式”可直接放大而不经过扩散采样（速度快，但细节补充较少）
  - 勾选“忽略提示词”可使用空提示词，让模型自由发挥，适合灵感探索

### 图像提示（Image Prompt）

上传参考图，让生成结果受其影响。支持四种控制方式：

- **ImagePrompt**：提取参考图的风格或内容
- **PyraCanny**：用边缘锁定构图，适合保持姿势或布局
- **CPDS**：保留空间结构和深度
- **FaceSwap**：保持面部一致性

每张参考图都可以独立调节：

- **停止步数**：控制网络在采样到哪一步时停止介入（数值越高影响越持久）
- **权重**：控制网络的影响强度

### 修复与扩图（Inpaint / Outpaint）

在图片上绘制区域（Mask），让模型重新绘制该区域。

- **修复（Inpaint）**：擦除并重绘指定区域
- **扩图（Outpaint）**：向图片外部扩展，自动补全新内容
  - 选择扩展方向（上下左右）
  - 模型会自动补全新区域

**高级用法——外部 Mask 上传**：
如果你在 Photoshop 等工具中准备好了精确的黑白掩码，可以：

1. 勾选“启用掩码上传”
2. 上传你的掩码图片
3. 掩码会以红色半透明叠加层显示在画布上，方便你确认位置
4. 如需补充，仍可在画布上手绘添加更多区域
5. 手绘和上传的掩码会合并处理

### 反推提示词（Describe）

上传一张图片，系统会自动分析并生成一段描述文字，填入提示词框。支持两种模式：

- 照片模式
- 艺术/动漫模式

### 加载元数据（Metadata）

如果你有一张之前用 ReFocus 生成的图片，里面嵌入了生成参数。上传后：

- 可以看到完整的参数记录
- 点击“应用元数据”，参数会自动加载到 UI 中，便于复现

---

## 高级设置（右侧面板）

勾选主界面的 **高级** 复选框，右侧会展开设置面板。

### 基本参数

- **步数**：数值越高，细节通常越好。1-10 步会自动切换到 LCM 极速模式
- **纵横比**：预置的宽高比列表
- **图像数量**：单次生成几张
- **反向提示词**：描述你不希望出现在画面中的内容
- **种子**：固定种子可复现同一结果，随机种子每次不同

### 模型与 LoRA

- 选择基础模型（仅 SDXL）和精炼器模型
- 精炼器切换步数：控制在第几步切换到精炼器
- 最多可同时加载 5 个 LoRA，每个可独立调节权重

### 高级调试工具

> ⚠️ **警告**
>
> 以下参数是为高级用户和调试场景准备的。**如果你不理解某个参数的含义，请不要改动它。** 大多数情况下，保持默认值就能得到很好的结果。错误调整可能导致生成质量下降或产生意外结果。

- **引导比例（CFG Scale）**：数值越高，生成结果越贴近提示词。默认值通常已经够用。
- **锐度**：控制画面清晰度。过度调整可能导致画面不自然。
- **输出格式**：选择保存图片的文件格式（png、jpg、webp）。
- **采样器 / 调度器**：手动选择采样算法。除非你清楚它们的区别，否则建议保持默认。
- **控制网络平滑度**：调整控制网络对图像的“柔化”程度。默认值适用于大多数情况。
- **Canny 阈值**：调整边缘检测的敏感度。仅在边缘控制不准确时需要调整。
- **FreeU**：实验性优化，可改善背景细节。效果因模型而异，需自行测试。

> 如果你只是想生成一张好看的图片，**不要碰上面这些参数**。它们的存在是为了解决特定的技术问题，而不是为了日常使用。

---

## 快捷键与技巧

- **点击生成后**：Stop 会完全终止，Skip 会跳过当前正在生成的一张
- **提示词框**：可直接粘贴包含 JSON 参数的文本，系统会自动识别并显示“加载参数”按钮
- **预设**：在设置面板中选择预设，可快速切换整套参数配置
- **历史记录**：所有生成都会保存在 `outputs/` 文件夹中，并生成一个 HTML 索引页面
